{
  "name": "claim-link-shortener",
  "version": "1.0.0",
  "private": true,
  "description": "A link shortener styled as a claim-ticket window, deployed on Netlify.",
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  }
}
